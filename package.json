{
  "name": "fast-lane-karting",
  "version": "1.0.0",
  "description": "Static Fast Lane Karting website, built with Vite, Pug and Stylus.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify-build.mjs",
    "update:fastest-lap": "node scripts/update-fastest-lap.cjs",
    "test": "node --test",
    "dev:worker": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "deploy:pages": "npm run build && wrangler pages deploy dist --config wrangler.pages.jsonc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lewisbooth/fast-lane-karting.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/lewisbooth/fast-lane-karting/issues"
  },
  "homepage": "https://github.com/lewisbooth/fast-lane-karting#readme",
  "dependencies": {
    "dotenv": "8.5.1",
    "google-spreadsheet": "3.1.15"
  },
  "private": true,
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "devDependencies": {
    "autoprefixer": "10.6.1",
    "postcss": "8.5.28",
    "pug": "3.0.4",
    "stylus": "0.64.0",
    "vite": "8.3.0",
    "wrangler": "4.137.0"
  }
}
