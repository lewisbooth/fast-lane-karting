include navigation
include footer

doctype html
html
  head
    meta(name="author" content="AMP Studio | https://amp.studio")

    block title
      title Fast Lane Karting Stoke | The Midlands' #1 Indoor Karting Experience
      meta(name="description" content="The best indoor karting venue in Stoke-on-Trent for 25 years. High performance 200cc sprint karts, kids karts & biz karts with on-site catering. Arrive & Drive today!")
      
    meta(http-equiv="Content-Type" content="text/html; charset=utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    meta(name="theme-color" content="#BB2024")

    //- Minimal favicon setup, covers most bases. 
    //- Remember to change favicon.ico in root folder
    link(rel="icon" type="image/png" sizes="16x16" href="/images/favicon/favicon-16x16.png")
    link(rel="icon" type="image/png" sizes="32x32" href="/images/favicon/favicon-32x32.png")
    link(rel="icon" type="image/png" sizes="96x96" href="/images/favicon/favicon-96x96.png")
    meta(name="msapplication-TileColor" content="#BB2024")
    meta(name="msapplication-TileImage" content="/images/favicon/ms-icon-144x144.png")

    //- Preload fonts & other assets that are linked from CSS here (such as background images), rather than wait for the browser to download and parse the CSS and request them. Much faster page loading speed in supported browsers.
    link(rel='preload' href='/fonts/BigNoodleTitling.woff2' as='font' type="font/woff2" crossorigin)

    //- Block for additional preloads that are specific to individual pages
    block pagePreloads

    //- Inline styles in production (change in gulpfile.js), remove this if CSS is over 40kb
    if production
      style
        include ../../../dist/css/style.css
    else
      link(rel='stylesheet' href='/css/style.css' type="text/css")
 
  body
    block nav
      +navigation

    block content

    block footer
      +footer

    block commonScripts
      script
        include ../../../dist/js/dollarSelect.js
        include ../../../dist/js/mobileMenu.js
        include ../../../dist/js/openingHours.js

    //- Extra scripts for individual pages
    block pageScripts